export type InflationData = {
    countryCode: string;
    countryName: string;
    timestamp: string;
    inflationRate: number;
};

export type IncomeData = {
    countryCode: string;
    countryName: string;
    timestamp: string;
    incomeValue: number;
};